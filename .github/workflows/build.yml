name: CI

on:
  push:
    branches: [ develop ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Print message
        run: echo building...

      - name: Build action
        run: |
          npx webpack 
          echo done.
